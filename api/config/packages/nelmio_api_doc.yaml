nelmio_api_doc:
    documentation:
        info:
            title: Solar CRM API
            description: API REST pour la gestion d'un CRM solaire photovoltaïque
            version: 1.0.0
        components:
            securitySchemes:
                bearerAuth:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - bearerAuth: []
    areas:
        path_patterns:
            - ^/api(?!/doc$)
